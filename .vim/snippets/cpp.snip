snippet ccrxinterval
    auto values = CCRx::interval(this, ${1})
    .subscribe([=](int v) {
        ${2}
    }, [](){printf("completed\n");});

snippet log
    CCLOG("${1}");

snippet cclog
    CCLOG("${1}: %${2}", ${3});

snippet foreach
    for (auto ${1}: ${2}) {
        ${3}
    }
