snippet log
    NSLog(@"${1}");${2}

snippet timer
    [NSTimer scheduledTimerWithTimeInterval:${1:0.1} target:self selector:@selector(${2:hoge:}) userInfo:nil repeats:${3:YES}];

snippet char_to_nsstr
    NSString *_${1:nsstr}       = [NSString stringWithUTF8String:<|1|> ? <|1|> : ""];

snippet if
    if(${1})
    {
        ${2}
    }
